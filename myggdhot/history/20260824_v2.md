# 뉴스레터 웹진 수정사항
2026-08-24 v2

# layerpopup.css (수정)
```css
@charset "utf-8";

/* [레이어 팝업] */
/* 이벤트 등 레이어 팝업 */
.lpop {
    position: absolute;
    top: 0;
    left: 0;
    z-index: 10;
    transform: translate(var(--_left, 0), var(--_top, 0));
}

.lpop.off {
    opacity: 0;
    pointer-events: none;
}

/* 개별 */
.lpop[data-popupid="lpop001"] {
    transform: translate(10px, calc(var(--hei-hdr) + 10px));
    width: clamp(200px, 100%, 540px);
    max-width: calc(100% - 20px);
}

/* ================================= */
/* 비쥬얼 영역 */
.lpop-visual {
    position: relative;
    width: 100%;
    vertical-align: middle;
}

/* ----------------------------- */
/* 이미지 */
.lpop-visualimg {
    position: relative;
    z-index: 2;
    /* user-select: none; */
}

/* 개별 */
.lpop-visual-img-lpop001-1 {
    width: 100%;
}

/* ===================================== */
/* ===================================== */
/* ===================================== */
/* [링크] */
.lpop-link {
    /* outline: 1px solid red; */
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    z-index: 3;
    text-decoration: none;
    font-weight: bold;
    font-size: clamp(14px, 2.4vw, 24px);
    transition: background .2s linear, color .2s linear;
    box-shadow: 
        -2px -2px 8px #4444440f,
        2px 2px 8px #44444420;
}

/* off */
.lpop-link.off {
    pointer-events: none;
}


/* 링크 개별 */
.lpop-link-lpop001 {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}


/* ===================================== */
/* ===================================== */
/* ===================================== */
/* [24시간 쿠키영역] */
.lpop-cookiewrap {
    display: flex;
    flex-flow: row wrap;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    position: relative;
    z-index: 4;
    width: 100%;
    height: 15%;
    padding: 0 0 0 10px;
    background: #fff;
    border: 1px solid #ddd;
    border-top: none;
    font-family: "SUITE-Bold";
    font-size: clamp(12px, 1.8vw, 16px);
}

/* 라벨 */
.lpop-lbl {
    display: flex;
    flex-flow: row nowrap;
    justify-content: flex-start;
    align-items: center;
    gap: .5em;
    position: relative;
    user-select: none;
    cursor:pointer;
}
.lpop-lbl:has(.lpop-chk:focus){
    outline:2px dotted black;
}
/* 체크박스 */
.lpop-chk {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    border: 0;
    clip: rect(0, 0, 0, 0);
    overflow: hidden;
    white-space: nowrap;
}

/* 체크박스 대용 ic */
.lpop-chkic {
    display: inline-block;
    position: relative;
    width: 1.3em;
    aspect-ratio: 1/1;
    background: #fff;
    border: 1px solid #bbbbbb;
    border-radius: 4px;
    pointer-events:none;
}

.lpop-chk:checked~.lpop-chkic {
    background: url("/cont/resources/myggd_hot/icons/ic_checkbox_checked.svg") no-repeat center center / cover;
    border-color: transparent;
}

/* 걍 라벨 텍스트 */
.lpop-chktxt {
    pointer-events: none;
    color: #333333;
}

/* 닫기 버튼 */
.lpop-close {
    position: relative;
    padding: .875em 1.4em;
    background: rgba(255, 255, 255, .5);
    border: none;
    border-radius: clamp(4px, 1vw, 9px);
    font-size: clamp(12px, 1.8vw, 16px);
    color: #333;
}
```